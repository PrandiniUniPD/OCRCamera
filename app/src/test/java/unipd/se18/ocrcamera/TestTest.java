package unipd.se18.ocrcamera;

import android.graphics.Bitmap;
import android.graphics.BitmapFactory;
import static org.junit.Assert.*;

import org.junit.Test;

/**public class TestTest {

    @Test
    public void rotationTest()
    {
        String path = this.getClass().getClassLoader().getResource("getTextFromFileTest.txt").getPath();
        BitmapFactory.Options bmOptions = new BitmapFactory.Options();
        Bitmap bitmapKK = BitmapFactory.decodeFile(path,bmOptions);
        Bitmap bitmaprotato = ResultActivity.rotateBitmap(bitmapKK, 0);
        assertEquals(bitmapKK, bitmaprotato);
    }
}
*/